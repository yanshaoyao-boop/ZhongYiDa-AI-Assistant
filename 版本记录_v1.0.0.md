# 仲易达智能助手 - 版本记录

## 当前版本：v1.0.0 (里程碑版本)

**日期：** 2026-02-26
**状态：** 核心工作流已打通 (文档上传 -> 向量化 -> RAG 检索对话)

---

### 🚀 已实现功能

1. **双端分离架构**：前端 (Vue 3 + Vite) 与后端 (FastAPI) 独立运行。
2. **知识库上传**：支持 Word (.docx) 和 PDF 文档的自动化解析与学习。
3. **多模态增强版向量化**：适配并调优了豆包 Vision 多模态向量模型 (250615版本)。
4. **智能对话 (RAG)**：支持根据上传的企业制度文档进行精准问答。
5. **结构化报价表**：初步支持 Excel 报价单的解析与缓存。
6. **桌面一键启动**：提供 `启动前端.bat` 和 `启动后端.bat`，解决 Windows 路径中文名导致的启动难题。

### 🛠️ 关键修复与优化

- **API适配**：解决了 Vision 模型返回格式 (Object vs List) 的差异，修复了 `KeyError: 0`。
- **编码兼容**：增加了文本清洗逻辑，自动过滤文档中的零宽空白字符，解决了 Windows 下的 `UnicodeEncodeError`。
- **逻辑加固**：重构了文档分块 (Chunking) 算法，增加了无限循环防御逻辑，解决了大文件解析时的内存溢出 (500 错误)。
- **网络优化**：修正了多模态向量接口地址为正确的 `/embeddings/multimodal`。

### 📋 运行环境配置

- **Python**: 3.12+ (虚拟环境 venv)
- **Node.js**: 最新稳定版
- **向量数据库**: ChromaDB (本地存储于 `backend/data/chromadb`)
- **大模型**: 豆包 (Doubao-pro-32k & Doubao-embedding-vision-250615)

---

### 💡 管理员提示

- 如需更新对话模型 ID，请修改 `backend/.env` 中的 `DOUBAO_MODEL_ENDPOINT`。
- 启动时，请确保两个黑色终端窗口均处于开启状态。
