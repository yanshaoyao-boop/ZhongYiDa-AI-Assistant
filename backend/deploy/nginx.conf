server {
    listen 80;
    server_name zhongyidazhinengzhushou.cn www.zhongyidazhinengzhushou.cn;

    # 允许用户上传大文件
    client_max_body_size 50M;

    # 前端静态文件
    location / {
        root /var/www/zyd-bot/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location = /api {
        return 301 /api/;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE/流式输出优化
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        proxy_read_timeout 300s;
    }
}

# 待配置 SSL 后可以启用以下部分
# server {
#     listen 443 ssl http2;
#     server_name zhongyidazhinengzhushou.cn www.zhongyidazhinengzhushou.cn;
#
#     ssl_certificate /etc/nginx/ssl/zhongyidazhinengzhushou.cn.pem;
#     ssl_certificate_key /etc/nginx/ssl/zhongyidazhinengzhushou.cn.key;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     client_max_body_size 50M;
#
#     location / {
#         root /var/www/zyd-bot/frontend/dist;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location = /api {
#         return 301 /api/;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8000/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_buffering off;
#         proxy_cache off;
#         chunked_transfer_encoding on;
#         proxy_set_header Connection '';
#         proxy_http_version 1.1;
#         proxy_read_timeout 300s;
#     }
# }
