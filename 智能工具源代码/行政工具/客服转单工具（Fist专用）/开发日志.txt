2026/3/11
1.不需要把 A 表所有内容都转到 B 表。所以设置10个映射字段就好了，这些字段由用户自己来选择。2.假设A表有5行数据，要求每一行填入B表生成一张独立的表格，也就是5行要生成5个表格，新生成表格以A表中F列（客户单号字段）对应的FBA码来命名。3.新生成的表格要完全复制B表格的模板，不论是公式、格式、颜色、布局等等。4.A表的AC列（收货件数）自动填到B表B16格子。5.A表的AH列（额外服务字段）内容填入B表F6格子，不要破坏该格子原有的公式。

1.把映射条数的上限提高到30个。2.A表F列固定填入B表A18（不需要在映射里选择），但是要遵循下列规则，填入B表A18的内容，由如下内容构成，F列的“FBA编码”+“U000001-”+“AC（收货件数）的数值”，例如：F是FBA123、AC是20，最终填入B表A18的内容是“FBA123U000001-20”。

2026/3/17
1. 增加一键插入图片功能，可选择将外部图片批量插入到 B 表指定的任意单元格。
2. 支持输出格式选择：除了默认的 .xlsx 格式，新增了对 .xls (Excel 97-2003) 格式的支持。
3. 后端功能增强：通过调用 Windows COM 接口实现高保真度的 .xls 格式转换，最大程度保留模板样式。